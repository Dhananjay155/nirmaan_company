import * as actions from './actions';
import * as types from './types';
import reducer, { initialState } from './reducer';

export { actions, types, reducer, initialState };
export default { actions, types, reducer, initialState };
